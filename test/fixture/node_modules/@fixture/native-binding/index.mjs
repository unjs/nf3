// The native bindings load `@fixture/native-libvips` at runtime via `@rpath`
// links in the compiled addon, so there is no JS reference to it here — it is
// only reachable through the `optionalDependencies` declared in package.json.
export default "@fixture/native-binding@1.0.0";
